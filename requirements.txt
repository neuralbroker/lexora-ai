# Lexora AI
# Enterprise Knowledge Intelligence Platform

## Core
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

## Database
sqlalchemy==2.0.25
asyncpg==0.29.0
psycopg2-binary==2.9.9
# NOTE: alembic removed (2026-09-14) — schema uses create_all; re-add with
# migrations/ when the first ALTER lands. See docs/database.md.

## Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.2

## LangChain & AI
langchain==0.2.17
langchain-community==0.2.19
langchain-openai==0.1.25
langchain-text-splitters==0.2.4
faiss-cpu==1.7.4
openai==1.44.0
tiktoken==0.7.0
huggingface-hub==0.23.0

## Document Processing
# pypdf is the PDF reader (app/utils/document_parser.py). pymupdf was removed
# (2026-09-14) — never imported anywhere.
pypdf==3.17.4
python-docx==1.1.0

## Caching & Async
redis==5.0.1
celery==5.3.6
httpx==0.26.0

## Logging & Monitoring
structlog==24.1.0
# NOTE: sentry-sdk removed (2026-09-14) — never wired (no init call anywhere).
# Re-add with SENTRY_DSN + init in lifespan when ready to use it.
prometheus-client==0.19.0

## Utilities
python-dotenv==1.0.0
pydantic==2.5.3
pydantic-settings==2.1.0
# tenacity backs LLM retries (app/services/llm_service.py).
tenacity==8.2.3
aiofiles==23.2.1
